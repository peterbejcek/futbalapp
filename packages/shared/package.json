{
  "name": "@fkknv/shared",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "//": "react-native/source: Metro (Expo) načíta @fkknv/shared priamo zo zdroja (src), takže mobil nepotrebuje build dist. Web (Next) a API (tsc) používajú main → dist.",
  "react-native": "./src/index.ts",
  "source": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@fkknv/config": "workspace:*",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
